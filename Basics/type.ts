// Type Annotations
let username: string = "Muzamil";
let age: number = 22;
let isAdmin: boolean = true;
console.log(username, age, isAdmin);

// Array and Tuples
// Array of numbers
let scores: number[] = [90, 80, 85];
// tuple (fixed types & length)
let user: [string, number] = ["Ali", 25];
console.log(scores, user);
